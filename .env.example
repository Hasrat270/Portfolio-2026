# Public site URL (used for metadata, sitemap, OG)
NEXT_PUBLIC_SITE_URL=https://hasrat.dev

# Optional: raises GitHub API rate limit (read-only PAT is fine)
GITHUB_TOKEN=

# Optional: enables real email delivery for /api/contact via Resend
RESEND_API_KEY=
CONTACT_FROM="Portfolio <onboarding@resend.dev>"
CONTACT_TO=zerovlabs18@gmail.com
